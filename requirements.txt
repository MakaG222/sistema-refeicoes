# Sistema de Refeições — Escola Naval
# Instalar com: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0        # geração e verificação de hashes de password
MarkupSafe>=2.1           # escape seguro de HTML nos templates

# ── Servidor WSGI para produção ───────────────────────────────────────────────
gunicorn>=22.0            # multi-worker, Railway/Docker ready

# ── Excel / exportações ───────────────────────────────────────────────────────
openpyxl>=3.1             # exportação .xlsx (obrigatório — não incluído no stdlib)

# ── Segurança / utilitários ───────────────────────────────────────────────────
# secrets, sqlite3, csv, shutil, pathlib, datetime — stdlib, não precisam de pip

# ── Desenvolvimento / qualidade (não instalar em produção) ────────────────────
# ruff                    # linter rápido (P3)
# pip-audit               # auditoria de vulnerabilidades (P3)
# pytest                  # testes (P3)
# pytest-flask            # fixtures Flask para testes (P3)
